import { BaseScreen } from "@/components/base-screen.component";

export class Auth extends BaseScreen{
    constructor() {
        super({title: 'Auth'})
    }
    render() {
        return '<p>Auth</p>'
    }
}